# R for travis: see documentation at https://docs.travis-ci.com/user/languages/r

language: R
cache: packages

matrix:
  include:
    addons:
      apt:
        sources:
          - sourceline: 'ppa:ubuntu'
        packages:
          - libmlpack-dev
